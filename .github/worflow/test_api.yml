name: Bruno CI/CD 

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  # ────────────────────── 1. Collection 1 (Get + Post) ──────────────────────
  collection:
    runs-on: ubuntu-latest
    name: CI - Collection 
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm install -g @usebruno/cli
      - name: Run Collection 1
        run: |
          bru run "collections \
            --env-file environments/Collection_env.bru \
            --format html \
            --output report-collection.html
      - uses: actions/upload-artifact@v4
        with:
          name: report-collection
          path: report-collection.html

  # ────────────────────── 3. CD – Déploiement Render (FIXED - No if conditions on secrets) ──────────────────────
  deploy-render:
    needs: [collection]
    runs-on: ubuntu-latest
    name: CD - Déploiement Auto → Render
    if: ${{ github.ref == 'refs/heads/main' && github.event_name == 'push' }}
    steps:
      - name: Déclenche le déploiement sur Render
        continue-on-error: true
        env:
          DEPLOY_HOOK: ${{ secrets.RENDER_DEPLOY_HOOK }}
        run: |
          if [ -z "$DEPLOY_HOOK" ]; then
            echo "⚠️  Aucun secret RENDER_DEPLOY_HOOK configuré"
            echo "Pour activer le CD : ajoute le secret dans Settings > Secrets > Actions"
            echo "Le déploiement est ignoré pour cette exécution."
            exit 0
          fi
          
          echo "🚀 Déploiement automatique de l'API en cours..."
          echo "URL : https://bfg-api-blood-demand-gl.onrender.com"
          
          RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" -X POST \
            "$DEPLOY_HOOK" \
            -H "Content-Type: application/json" \
            -d '{"clearCache": true}' || echo "curl_failed")
          
          if [[ "$RESPONSE" == "2"* ]] || [[ "$RESPONSE" == "3"* ]]; then
            echo "✅ Déploiement déclenché avec succès ! (HTTP $RESPONSE)"
          else
            echo "⚠️  Attention : Réponse inattendue du webhook Render (HTTP $RESPONSE)"
            exit 1
          fi